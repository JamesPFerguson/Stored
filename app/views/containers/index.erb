<% if @containers %>

  <% @containers.each do |container| %>
    <h3> Container: <a href="/containers/<%=container.id%>"><%=container.name%></a></h3>
  <%end%>

<% else %>

    <h2>You have not added any containers.</h2>

<%end%>
